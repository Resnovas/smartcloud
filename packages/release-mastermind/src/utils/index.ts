import * as labels from './labels'
import * as parsingData from './parsingData'
import * as respond from './respond'
import * as versioning from './versioning'

export const utils = {
  parsingData,
  versioning,
  respond,
  labels
}
