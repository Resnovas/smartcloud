name: 'Convention Mastermind'
description: 'Enforce Conventions how and when you want, the way you want.'
author: 'Videndum'
inputs:
  GITHUB_TOKEN:
    description: 'The GITHUB_TOKEN secret'
    required: true
  config:
    description: 'Config file path'
    default: '.github/config.json'
    required: false
  configJSON:
    description: 'JSON string with configuration data'
    default: ''
    required: false
runs:
  using: 'node12'
  main: 'dist/index.js'
branding:
  color: 'purple'
  icon: 'tag'
